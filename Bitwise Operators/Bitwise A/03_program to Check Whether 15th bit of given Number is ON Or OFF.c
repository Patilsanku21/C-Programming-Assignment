#include<stdio.h>
#include<conio.h>
int main()
{
    int No = 0,BNo = 0;

    printf("\n Enter a Number :");
    scanf("%d",&No);

    printf("\n Enter a Bit Number :");
    scanf("%d",&BNo);

    if(BNo > 32 || BNo < 1)
    {
        printf("\n Invalid Bit Number \n");
    }
    if(BNo > 32)
    {

    }
}
